const _ = require('lodash');

var self = {
    id: 'independent',
    name: 'Independent',
    description: `You win only when you are the last one standing!`,
    isVictory: (p) => {
    },
};
module.exports = self;